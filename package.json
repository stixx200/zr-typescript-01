{
  "name": "zr-typescript-01",
  "version": "1.0.0",
  "description": "- `npm init -y` - `npm install --save-dev typescript` - `npx tsc --init`",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript": "^5.2.2"
  }
}
